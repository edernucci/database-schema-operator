apiVersion: db.pedag.io/v1
kind: Table
metadata:
  name: addresses-table
spec:
  name: Addresses
  columns:
  - name: Street
    type: varchar(50)
  - name: Number
    type: varchar(50)
  - name: UserId
    type: int
